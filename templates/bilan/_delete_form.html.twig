<form method="post" action="{{ path('app_bilan_delete', {'id': bilan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bilan.id) }}">
    <button class="btn">Delete</button>
</form>
